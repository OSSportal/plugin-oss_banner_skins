<div class="link_mseminar">
    <a href="{{ url($item->link) }}" target="{{ $item->link_target }}">
        <span><img src="{{ $item->imageUrl() }}" width="100%" height="169" alt="{{ $item->title }}" role="presentation"></span>
    </a>
</div>